[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "botzapp"
version = "0.1.0"
description = "Local BotZapp chat app"
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
botzapp = "app.interfaces.cli.main:main"
